"""Flask blueprints for BookAppt.ai routes.

Blueprints will be added in later phases:
  - dashboard.py      (Phase 5) — main page with booking history
  - records.py        (Phase 3) — business records CRUD
  - bookings.py       (Phase 4) — create booking, place call, view detail
  - calendar_auth.py  (Phase 2) — Google Calendar OAuth flow
  - alerts.py         (Phase 6) — pending approvals, hold management
"""
